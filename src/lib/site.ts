/**
 * Update this once the site has a real domain — it feeds metadataBase,
 * robots.ts and sitemap.ts so there's a single place to change it.
 */
export const SITE_URL = "https://portfolio-nasif.vercel.app";
